# Quiz

1. เพราะเหตุใดควรตรวจ Official API ก่อนเขียน HTML scraper?
2. ควรกำหนด timeout และ rate limit เพื่อป้องกันปัญหาใด?
3. Fixture และ mock HTTP response ช่วยให้ test มีคุณสมบัติใด?
4. เหตุใดไม่ควรใส่ token ใน command line ของ scheduled task?
5. Exit code `2` ของ Phase 13 หมายถึงอะไร?
6. ข้อมูลใดควรอยู่ใน raw layer และข้อมูลใดควรอยู่ใน processed layer?
7. ก่อน download เอกสารจากเว็บไซต์ควรตรวจเงื่อนไขใดบ้าง?
8. ใน CI ควรใช้ permissions เริ่มต้นแบบใด?
9. Vercel deployment ที่เตรียมใน Phase 15 ใช้สำหรับส่วนใดของ repository?
10. หาก parser พบ selector หาย ควร fail fast หรือ graceful degradation เมื่อใด?
